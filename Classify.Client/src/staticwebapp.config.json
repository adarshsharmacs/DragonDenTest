{
    "navigationFallback": {
        "rewrite": "/index.html"
    },
    "routes": [
        {
            "route": "/assets/*",
            "headers": {
                "cache-control": "public, max-age=604800, immutable"
            }
        }
    ]
}